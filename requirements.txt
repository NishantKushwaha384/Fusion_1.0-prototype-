# ============================================================
# FUSION 1.0 — PYTHON DEPENDENCIES
# ============================================================
# Install everything with:
#   pip install -r requirements.txt
#
# What each library does:
#   fastapi      — Web framework (creates the API server)
#   uvicorn      — Runs the FastAPI server
#   pydantic     — Data validation (request/response models)
#   groq         — Groq API client (free Llama3 calls)
#   openai       — OpenAI API client (GPT-4o mini)
#   google-generativeai — Gemini API client
#   python-dotenv — Reads .env file for API keys
#   httpx        — Async HTTP client (used internally)
# ============================================================

fastapi==0.111.0
uvicorn==0.30.1
pydantic==2.7.4
groq==0.9.0
google-generativeai==0.7.2
python-dotenv==1.0.1
httpx==0.27.0
ollama==0.3.4
requests==2.31.0
